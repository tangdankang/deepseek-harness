/**
 * @deepseek-ai/dsh-vscode-app — the VS Code Host API profile bundle. Its
 * runtime content is the patch declared by `dsh.bundle.patch`; this module
 * intentionally exposes no plugin API.
 * @module @deepseek-ai/dsh-vscode-app
 */

export {}
